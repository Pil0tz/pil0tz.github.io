# To Do

- [ ] padding in header should not be the same as the main container, instead it should flex with the site info on the left, links to different pages on the right (cv & portfolio), and the language switcher on the right
- [ ] language switch color should be border, not background. it can be a bit thick but not too much
- [ ] padding and alignment of lang switcher is different depending on which country is selected. this needs to not happen, there's been a chat with Claude previously that fixed this, but it somehow reverted again. It probably has to do with the alignment of the actual text inside the span